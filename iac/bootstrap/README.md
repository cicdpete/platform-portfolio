# iac/bootstrap

Terraform to install **Argo CD** on an existing Minikube cluster (`kubernetes` / `helm` providers).

**Prerequisite:** `minikube start` and correct `kubectl` context.

Implementation: **TBD**. See [local golden path](../../docs/runbooks/local-golden-path.md).
